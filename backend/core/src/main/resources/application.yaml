spring:
  application:
    name: core
  datasource:
    url: jdbc:postgresql://localhost:5432/smartedu
    username: postgres
    password: root
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    enabled: true
jwt:
  secret: ${JWT_SECRET:dev-only-secret-dev-only-secret-dev-only-secret-64chars!!}
  access-ttl-hours: 24
server:
  port: 8080